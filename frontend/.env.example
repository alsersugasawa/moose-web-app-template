# Copy to .env.local and set values for your environment.

# API base URL — leave empty to use Vite's dev-server proxy (recommended for development).
# Set to the full backend URL only when building for a separate deployment.
VITE_API_BASE=
